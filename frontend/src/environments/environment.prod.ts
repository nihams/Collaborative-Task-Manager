export const environment = {
  production: true,
  // Same-origin proxy path in production (see deployment guide).
  // If you deploy without a proxy, put your full backend URL here instead.
  apiUrl: '',
  wsUrl: 'https://your-backend-url.onrender.com',
};
